{
  "name": "nassau",
  "private": true,
  "packageManager": "pnpm@9.12.3",
  "scripts": {
    "dev": "pnpm --parallel --filter @nassau/client --filter @nassau/api dev",
    "build": "pnpm -r build",
    "test": "pnpm --filter @nassau/game-engine test",
    "lint": "pnpm -r lint",
    "format": "prettier --write ."
  },
  "devDependencies": {
    "prettier": "^3.3.3",
    "typescript": "^5.6.3"
  }
}
