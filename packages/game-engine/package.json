{
  "name": "@nassau/game-engine",
  "version": "0.1.0",
  "private": true,
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "test": "vitest run",
    "build": "tsc",
    "lint": "tsc --noEmit"
  },
  "devDependencies": {
    "vitest": "^2.1.5"
  }
}
